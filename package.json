{
  "name": "@redux-auth-examples/monorepo",
  "version": "1.0.0",
  "description": "Example of how-to handle authentication with Redux",
  "private": true,
  "scripts": {
    "build": "pnpm -r --filter {packages} run build",
    "lint": "pnpm -r --parallel run lint",
    "prepare": "husky install",
    "test": "pnpm -r --parallel run test"
  },
  "keywords": [
    "redux"
  ],
  "author": "Jaime Leonardo Suncin Cruz",
  "license": "GPL-3.0",
  "devDependencies": {
    "@commitlint/cli": "^16.2.1",
    "@commitlint/config-conventional": "^16.2.1",
    "husky": "^7.0.4",
    "lint-staged": "^12.3.4",
    "prettier": "^2.5.1"
  }
}
